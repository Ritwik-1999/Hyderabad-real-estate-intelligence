# Repository Setup

Suggested repository name:

`hyderabad-real-estate-intelligence`

Suggested GitHub description:

`Hyderabad-focused real estate intelligence platform combining pricing, RERA supply, infrastructure, and GIS signals for market analysis and growth forecasting.`

Suggested topics:

`real-estate`
`data-engineering`
`geospatial`
`gis`
`postgis`
`python`
`fastapi`
`analytics`
`hyderabad`
`proptech`
`forecasting`

Recommended visibility:

Public documentation / showcase repository.

Keep production code, credentials, licensed datasets, source-specific ingestion logic, and private research data in the existing private repository.
